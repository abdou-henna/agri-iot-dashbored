import type { AgronomicIntelligenceOutput } from '../../types/agronomicIntelligence';

export function AgronomicIntelligencePanel({ data }: { data: AgronomicIntelligenceOutput }) {
  return <section className="rounded-lg border border-slate-200 bg-white p-4 space-y-2"><h2 className="text-base font-semibold">Deterministic Agronomic Intelligence</h2><p className="text-sm">Farm state: {data.farm_state.season_context}; freshness {data.farm_state.data_freshness_minutes ?? 'n/a'} min.</p><p className="text-sm">Pivot: moisture divergence {data.pivot_intelligence.moisture_divergence_percent ?? 'n/a'}%.</p><p className="text-sm">Irrigation response: {data.irrigation_reasoning.response_flag}.</p><p className="text-sm">Cutting/regrowth: {data.cutting_regrowth_context.regrowth_context_label}.</p><p className="text-sm">Fertilization context: {data.fertilization_context.ec_relative_trend_context}.</p><p className="text-xs">Confidence: {data.reliability.level} ({data.reliability.score ?? 'n/a'})</p><ul className="list-disc pl-5 text-xs">{data.limitations.slice(0, 6).map((l) => <li key={l}>{l}</li>)}</ul><p className="text-xs text-slate-500">AI Interpretation is shown separately below.</p></section>;
}
